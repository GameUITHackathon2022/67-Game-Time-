using System.Collections;
using System.Collections.Generic;
using UnityEngine;
//using UnityEngine.Experimental.U2D.Animation;

[System.Obsolete("Don't use this anymore")]
public class NetworkCallBack : MonoBehaviour 
{
    // public GameObject squarePrefab;
    // public override void SceneLoadLocalDone(string scene, IProtocolToken token)
    // {
    //     Vector2 spawnPos = new Vector2(Random.Range(-2f, 2f), Random.Range(-2f, 3f));
    //     BoltEntity playerEntity = BoltNetwork.Instantiate(squarePrefab, spawnPos, Quaternion.identity);
    //     playerEntity.GetComponent<SpriteLibrary>().spriteLibraryAsset = GameMananger.instance.CharacterSpriteLibraryAssets[Random.Range(0,4)];
    //     base.SceneLoadLocalDone(scene, token);
    // }
    // // Start is called before the first frame update
    // void Start()
    // {

    // }

    // // Update is called once per frame
    // void Update()
    // {

    // }
}
